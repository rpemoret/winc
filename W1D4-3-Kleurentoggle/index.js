const content = document.getElementById("newbutton");

const button = document.getElementById("button");

button.onclick = function {

    if (content.className == "open") { content.className = "" }
    else { content.className == "open" }
}